apiVersion: batch/v1
kind: Job
metadata:
  name: ocr-train-ranged-7-10-blarge
spec:
  template:
    metadata:
      labels:
        app: ocr
    spec:
      restartPolicy: Never
      containers:
        - name: lpr
          image: registry.datexis.com/tsteffek/licenseplatereconstructor:b0e72c70b1982d306e3720afa417e0a1cf4f8d01
          command: [ "python" ]
          args: [ "bin/TrainOCR.py",
                  "--path", "/var/lib/images",
                  "--width_mult", "0.5",
                  "--early_stopping",
                  "--max_epochs", "200",
                  "--min_epochs", "50",
                  "--default_root_dir", "/var/lib/models/range-7-10",
                  "--batch_size", "2500",
                  "--auto_scale_batch_size", "binsearch",
                  "--lr", "4e-4",
                  "--gpus", "-1",
                  "--precision", "16",
                  "--target_size", "0.2",
                  "--progress_bar_refresh_ratio", "0.1",
          ]
          resources:
            requests:
              nvidia.com/gpu: 1
              memory: 200Gi
            limits:
              nvidia.com/gpu: 1
              memory: 300Gi
          volumeMounts:
            - name: ocr-images
              mountPath: /var/lib/images
            - name: ocr-models
              mountPath: /var/lib/models
            - name: dshm
              mountPath: /dev/shm
      volumes:
        - name: ocr-models
          persistentVolumeClaim:
            claimName: ocr-models-pvc
        - name: ocr-images
          persistentVolumeClaim:
            claimName: ocr-images-pvc-ranged-7-10
        - name: dshm
          emptyDir:
            medium: Memory
      imagePullSecrets:
        - name: private-registry-auth
      nodeSelector:
        gpu: a100