apiVersion: batch/v1
kind: Job
metadata:
  name: ocr-train-ranged-7-10-blarge
spec:
  template:
    metadata:
      labels:
        app: ocr
    spec:
      restartPolicy: Never
      containers:
        - name: lpr
          image: registry.datexis.com/tsteffek/licenseplatereconstructor:3aab6778effd0cea919861f0d55fcf98f38d66e6
          command: [ "python" ]
          args: [ "bin/TrainOCR.py",
                  "--path", "/var/lib/images",
                  "--width_mult", "0.5",
                  "--early_stopping",
                  "--max_epochs", "300",
                  "--min_epochs", "50",
                  "--default_root_dir", "/var/lib/models/range-7-10",
                  "--batch_size", "3000",
                  "--lr", "3e-4",
                  "--gpus", "-1",
                  "--precision", "16",
                  "--target_size", "0.2",
                  "--progress_bar_refresh_ratio", "0.1",
            #                  "--auto_scale_batch_size", "binsearch",
                  "--accelerator", "ddp"
          ]
          resources:
            requests:
              nvidia.com/gpu: 2
              memory: 200Gi
            limits:
              nvidia.com/gpu: 2
              memory: 300Gi
          volumeMounts:
            - name: ocr-images
              mountPath: /var/lib/images
            - name: ocr-models
              mountPath: /var/lib/models
            - name: dshm
              mountPath: /dev/shm
      volumes:
        - name: ocr-models
          persistentVolumeClaim:
            claimName: ocr-models-pvc
        - name: ocr-images
          persistentVolumeClaim:
            claimName: ocr-images-pvc-ranged-7-10
        - name: dshm
          emptyDir:
            medium: Memory
      imagePullSecrets:
        - name: private-registry-auth
      nodeSelector:
        gpu: a100